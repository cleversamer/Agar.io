module.exports = {
  defaultOrbs: 5000,
  defaultSpeed: 6,
  defaultSize: 6,
  defaultZoom: 1.5, // as the player gets bigger, the zoom needs to go out
  worldWidth: 5000,
  worldHeight: 5000,
  fps: 30,
};
